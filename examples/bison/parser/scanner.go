// Code generated by GoLR. DO NOT EDIT.
package parser

import (
	"errors"
	"fmt"
	"golr/pkg/runtime"
)

var (
	// ErrInvalidRune is an error which is returned when no transition matches the current rune.
	ErrInvalidRune = errors.New("invalid rune")
)

const (
	// InvalidToken is a terminal which does not exist. It is used for situations where no token was found yet.
	InvalidToken Terminal = ^Terminal(0)
)

// Scanner implements the scanner and returns tokens.
type Scanner struct {
	runeReader runtime.UTF8RuneReader
	tokenStart runtime.UTF8RuneReader
	tokenEnd   runtime.UTF8RuneReader

	state int
	token Terminal

	err error
}

// NewScanner creates a new instance of the scanner with the given rune reader.
func NewScanner(runeReader runtime.UTF8RuneReader) Scanner {
	// consume the first rune here, as the start state does not call Next() on its own
	_ = runeReader.Next()
	return Scanner{
		runeReader: runeReader,
		tokenStart: runeReader,
		tokenEnd:   runeReader,
		token:      InvalidToken,
	}
}

// Err returns the error which occurred on the last call to Next().
// It will return io.EOF after Next() has returned false.
func (s *Scanner) Err() error {
	return s.err
}

// Token returns the current token.
func (s *Scanner) Token() Terminal {
	return s.token
}

// ByteOffset returns the start of the token in bytes from the source start.
// It returns the location after the last byte after the call to Next() returned false.
func (s *Scanner) ByteOffset() int {
	return s.tokenStart.ByteOffset()
}

// Line returns the line the token is located on.
func (s *Scanner) Line() int {
	return s.tokenStart.Line()
}

// Column returns the column the token is located on.
func (s *Scanner) Column() int {
	return s.tokenStart.Column()
}

// Lexeme returns the bytes which make up the token.
func (s *Scanner) Lexeme() []byte {
	return s.tokenStart.Lexeme(s.tokenStart.ByteOffset(), s.tokenEnd.ByteOffset())
}

// Next consumes characters until it found the next token.
func (s *Scanner) Next() bool {
	s.err = nil
	s.tokenStart = s.tokenEnd
	s.runeReader = s.tokenEnd
	s.state = 0

	for s.err == nil {
		s.err = s.dispatchState()
	}

	if s.tokenStart.ByteOffset() != s.tokenEnd.ByteOffset() {
		s.err = nil
		return true
	}
	s.token = InvalidToken
	return false
}

func (s *Scanner) dispatchState() error {
	switch s.state {
	case 0:
		return s.state0Ws()
	case 1:
		return s.state1Ws()
	case 2:
		return s.state2Ws()
	case 3:
		return s.state3Comment()
	case 4:
		return s.state4String()
	case 5:
		return s.state5Tstring()
	case 6:
		return s.state6PercentToken()
	case 7:
		return s.state7Colon()
	case 8:
		return s.state8Equal()
	case 9:
		return s.state9Pipe()
	case 10:
		return s.state10Semicolon()
	case 11:
		return s.state11IntLiteral()
	case 12:
		return s.state12Comment()
	case 13:
		return s.state13Comment()
	case 14:
		return s.state14String()
	case 15:
		return s.state15String()
	case 16:
		return s.state16Tstring()
	case 17:
		return s.state17BracedCode()
	case 18:
		return s.state18PercentToken()
	case 19:
		return s.state19PercentNterm()
	case 20:
		return s.state20PercentDestructor()
	case 21:
		return s.state21PercentPrinter()
	case 22:
		return s.state22PercentLeft()
	case 23:
		return s.state23PercentRight()
	case 24:
		return s.state24PercentMerge()
	case 25:
		return s.state25PercentCode()
	case 26:
		return s.state26PercentErrorVerbose()
	case 27:
		return s.state27PercentFilePrefix()
	case 28:
		return s.state28PercentFlag()
	case 29:
		return s.state29PercentGlrParser()
	case 30:
		return s.state30PercentHeader()
	case 31:
		return s.state31PercentInitialAction()
	case 32:
		return s.state32PercentOutput()
	case 33:
		return s.state33PercentSkeleton()
	case 34:
		return s.state34PercentVerbose()
	case 35:
		return s.state35PercentYacc()
	case 36:
		return s.state36PercentPercent()
	case 37:
		return s.state37PercentUnion()
	case 38:
		return s.state38Comment()
	case 39:
		return s.state39String()
	case 40:
		return s.state40Tstring()
	case 41:
		return s.state41BracedCode()
	case 42:
		return s.state42PercentToken()
	case 43:
		return s.state43PercentType()
	case 44:
		return s.state44PercentNterm()
	case 45:
		return s.state45PercentNonassoc()
	case 46:
		return s.state46PercentNamePrefix()
	case 47:
		return s.state47PercentDestructor()
	case 48:
		return s.state48PercentDprec()
	case 49:
		return s.state49PercentPrinter()
	case 50:
		return s.state50PercentPureParser()
	case 51:
		return s.state51PercentParam()
	case 52:
		return s.state52PercentLeft()
	case 53:
		return s.state53PercentLanguage()
	case 54:
		return s.state54PercentRight()
	case 55:
		return s.state55PercentRequire()
	case 56:
		return s.state56PercentMerge()
	case 57:
		return s.state57PercentCode()
	case 58:
		return s.state58PercentErrorVerbose()
	case 59:
		return s.state59PercentExpect()
	case 60:
		return s.state60PercentEmpty()
	case 61:
		return s.state61PercentFilePrefix()
	case 62:
		return s.state62PercentFlag()
	case 63:
		return s.state63PercentGlrParser()
	case 64:
		return s.state64PercentHeader()
	case 65:
		return s.state65PercentInitialAction()
	case 66:
		return s.state66PercentOutput()
	case 67:
		return s.state67PercentSkeleton()
	case 68:
		return s.state68PercentStart()
	case 69:
		return s.state69PercentVerbose()
	case 70:
		return s.state70PercentYacc()
	case 71:
		return s.state71PercentUnion()
	case 72:
		return s.state72Comment()
	case 73:
		return s.state73Tstring()
	case 74:
		return s.state74Tstring()
	case 75:
		return s.state75BracedCode()
	case 76:
		return s.state76PercentToken()
	case 77:
		return s.state77PercentType()
	case 78:
		return s.state78PercentNterm()
	case 79:
		return s.state79PercentNonassoc()
	case 80:
		return s.state80PercentNoDefaultPrec()
	case 81:
		return s.state81PercentNamePrefix()
	case 82:
		return s.state82PercentDestructor()
	case 83:
		return s.state83PercentDefaultPrec()
	case 84:
		return s.state84PercentDprec()
	case 85:
		return s.state85PercentPrinter()
	case 86:
		return s.state86PercentPrecedence()
	case 87:
		return s.state87PercentPureParser()
	case 88:
		return s.state88PercentParam()
	case 89:
		return s.state89PercentLeft()
	case 90:
		return s.state90PercentLanguage()
	case 91:
		return s.state91PercentRight()
	case 92:
		return s.state92PercentRequire()
	case 93:
		return s.state93PercentMerge()
	case 94:
		return s.state94PercentCode()
	case 95:
		return s.state95PercentErrorVerbose()
	case 96:
		return s.state96PercentExpect()
	case 97:
		return s.state97PercentEmpty()
	case 98:
		return s.state98PercentFilePrefix()
	case 99:
		return s.state99PercentFlag()
	case 100:
		return s.state100PercentGlrParser()
	case 101:
		return s.state101PercentHeader()
	case 102:
		return s.state102PercentInitialAction()
	case 103:
		return s.state103PercentOutput()
	case 104:
		return s.state104PercentSkeleton()
	case 105:
		return s.state105PercentStart()
	case 106:
		return s.state106PercentVerbose()
	case 107:
		return s.state107PercentYacc()
	case 108:
		return s.state108PercentUnion()
	case 109:
		return s.state109Tstring()
	case 110:
		return s.state110Tstring()
	case 111:
		return s.state111BracedCode()
	case 112:
		return s.state112PercentToken()
	case 113:
		return s.state113PercentType()
	case 114:
		return s.state114PercentNterm()
	case 115:
		return s.state115PercentNonassoc()
	case 116:
		return s.state116PercentNondeterministicParser()
	case 117:
		return s.state117PercentNoDefaultPrec()
	case 118:
		return s.state118PercentNoLines()
	case 119:
		return s.state119PercentNamePrefix()
	case 120:
		return s.state120PercentDestructor()
	case 121:
		return s.state121PercentDefaultPrec()
	case 122:
		return s.state122PercentDefine()
	case 123:
		return s.state123PercentDprec()
	case 124:
		return s.state124PercentPrinter()
	case 125:
		return s.state125PercentPrec()
	case 126:
		return s.state126PercentPureParser()
	case 127:
		return s.state127PercentParam()
	case 128:
		return s.state128PercentLeft()
	case 129:
		return s.state129PercentLanguage()
	case 130:
		return s.state130PercentRight()
	case 131:
		return s.state131PercentRequire()
	case 132:
		return s.state132PercentMerge()
	case 133:
		return s.state133PercentCode()
	case 134:
		return s.state134PercentErrorVerbose()
	case 135:
		return s.state135PercentExpect()
	case 136:
		return s.state136PercentEmpty()
	case 137:
		return s.state137PercentFilePrefix()
	case 138:
		return s.state138PercentFlag()
	case 139:
		return s.state139PercentGlrParser()
	case 140:
		return s.state140PercentHeader()
	case 141:
		return s.state141PercentInitialAction()
	case 142:
		return s.state142PercentOutput()
	case 143:
		return s.state143PercentSkeleton()
	case 144:
		return s.state144PercentStart()
	case 145:
		return s.state145PercentVerbose()
	case 146:
		return s.state146PercentYacc()
	case 147:
		return s.state147PercentUnion()
	case 148:
		return s.state148Tstring()
	case 149:
		return s.state149BracedCode()
	case 150:
		return s.state150PercentToken()
	case 151:
		return s.state151PercentNterm()
	case 152:
		return s.state152PercentNonassoc()
	case 153:
		return s.state153PercentNondeterministicParser()
	case 154:
		return s.state154PercentNoDefaultPrec()
	case 155:
		return s.state155PercentNoLines()
	case 156:
		return s.state156PercentNamePrefix()
	case 157:
		return s.state157PercentDestructor()
	case 158:
		return s.state158PercentDefaultPrec()
	case 159:
		return s.state159PercentDefine()
	case 160:
		return s.state160PercentDprec()
	case 161:
		return s.state161PercentPrinter()
	case 162:
		return s.state162PercentPrecedence()
	case 163:
		return s.state163PercentPureParser()
	case 164:
		return s.state164PercentParam()
	case 165:
		return s.state165PercentLanguage()
	case 166:
		return s.state166PercentRight()
	case 167:
		return s.state167PercentRequire()
	case 168:
		return s.state168PercentMerge()
	case 169:
		return s.state169PercentErrorVerbose()
	case 170:
		return s.state170PercentExpect()
	case 171:
		return s.state171PercentEmpty()
	case 172:
		return s.state172PercentFilePrefix()
	case 173:
		return s.state173PercentFlag()
	case 174:
		return s.state174PercentGlrParser()
	case 175:
		return s.state175PercentHeader()
	case 176:
		return s.state176PercentInitialAction()
	case 177:
		return s.state177PercentOutput()
	case 178:
		return s.state178PercentSkeleton()
	case 179:
		return s.state179PercentStart()
	case 180:
		return s.state180PercentVerbose()
	case 181:
		return s.state181PercentUnion()
	case 182:
		return s.state182BracedCode()
	case 183:
		return s.state183PercentTokenTable()
	case 184:
		return s.state184PercentNonassoc()
	case 185:
		return s.state185PercentNondeterministicParser()
	case 186:
		return s.state186PercentNoDefaultPrec()
	case 187:
		return s.state187PercentNoLines()
	case 188:
		return s.state188PercentNamePrefix()
	case 189:
		return s.state189PercentDestructor()
	case 190:
		return s.state190PercentDefaultPrec()
	case 191:
		return s.state191PercentDefine()
	case 192:
		return s.state192PercentPrinter()
	case 193:
		return s.state193PercentPrecedence()
	case 194:
		return s.state194PercentPureParser()
	case 195:
		return s.state195PercentLanguage()
	case 196:
		return s.state196PercentRequire()
	case 197:
		return s.state197PercentErrorVerbose()
	case 198:
		return s.state198PercentExpect()
	case 199:
		return s.state199PercentFilePrefix()
	case 200:
		return s.state200PercentFlag()
	case 201:
		return s.state201PercentGlrParser()
	case 202:
		return s.state202PercentHeader()
	case 203:
		return s.state203PercentInitialAction()
	case 204:
		return s.state204PercentOutput()
	case 205:
		return s.state205PercentSkeleton()
	case 206:
		return s.state206PercentVerbose()
	case 207:
		return s.state207BracedCode()
	case 208:
		return s.state208PercentTokenTable()
	case 209:
		return s.state209PercentNonassoc()
	case 210:
		return s.state210PercentNondeterministicParser()
	case 211:
		return s.state211PercentNoDefaultPrec()
	case 212:
		return s.state212PercentNoLines()
	case 213:
		return s.state213PercentNamePrefix()
	case 214:
		return s.state214PercentDestructor()
	case 215:
		return s.state215PercentDefaultPrec()
	case 216:
		return s.state216PercentPrinter()
	case 217:
		return s.state217PercentPrecedence()
	case 218:
		return s.state218PercentPureParser()
	case 219:
		return s.state219PercentLanguage()
	case 220:
		return s.state220PercentRequire()
	case 221:
		return s.state221PercentErrorVerbose()
	case 222:
		return s.state222PercentExpectRr()
	case 223:
		return s.state223PercentFilePrefix()
	case 224:
		return s.state224PercentGlrParser()
	case 225:
		return s.state225PercentInitialAction()
	case 226:
		return s.state226PercentSkeleton()
	case 227:
		return s.state227PercentVerbose()
	case 228:
		return s.state228BracedCode()
	case 229:
		return s.state229PercentTokenTable()
	case 230:
		return s.state230PercentNonassoc()
	case 231:
		return s.state231PercentNondeterministicParser()
	case 232:
		return s.state232PercentNoDefaultPrec()
	case 233:
		return s.state233PercentNoLines()
	case 234:
		return s.state234PercentNamePrefix()
	case 235:
		return s.state235PercentDestructor()
	case 236:
		return s.state236PercentDefaultPrec()
	case 237:
		return s.state237PercentPrecedence()
	case 238:
		return s.state238PercentPureParser()
	case 239:
		return s.state239PercentLanguage()
	case 240:
		return s.state240PercentErrorVerbose()
	case 241:
		return s.state241PercentExpectRr()
	case 242:
		return s.state242PercentFilePrefix()
	case 243:
		return s.state243PercentGlrParser()
	case 244:
		return s.state244PercentInitialAction()
	case 245:
		return s.state245PercentSkeleton()
	case 246:
		return s.state246BracedCode()
	case 247:
		return s.state247PercentTokenTable()
	case 248:
		return s.state248PercentNondeterministicParser()
	case 249:
		return s.state249PercentNoDefaultPrec()
	case 250:
		return s.state250PercentNamePrefix()
	case 251:
		return s.state251PercentDestructor()
	case 252:
		return s.state252PercentDefaultPrec()
	case 253:
		return s.state253PercentPrecedence()
	case 254:
		return s.state254PercentPureParser()
	case 255:
		return s.state255PercentErrorVerbose()
	case 256:
		return s.state256PercentExpectRr()
	case 257:
		return s.state257PercentFilePrefix()
	case 258:
		return s.state258PercentGlrParser()
	case 259:
		return s.state259PercentInitialAction()
	case 260:
		return s.state260BracedCode()
	case 261:
		return s.state261PercentTokenTable()
	case 262:
		return s.state262PercentNondeterministicParser()
	case 263:
		return s.state263PercentNoDefaultPrec()
	case 264:
		return s.state264PercentNamePrefix()
	case 265:
		return s.state265PercentDestructor()
	case 266:
		return s.state266PercentDefaultPrec()
	case 267:
		return s.state267PercentPrecedence()
	case 268:
		return s.state268PercentPureParser()
	case 269:
		return s.state269PercentErrorVerbose()
	case 270:
		return s.state270PercentFilePrefix()
	case 271:
		return s.state271PercentGlrParser()
	case 272:
		return s.state272PercentInitialAction()
	case 273:
		return s.state273BracedCode()
	case 274:
		return s.state274PercentTokenTable()
	case 275:
		return s.state275PercentNondeterministicParser()
	case 276:
		return s.state276PercentNoDefaultPrec()
	case 277:
		return s.state277PercentNamePrefix()
	case 278:
		return s.state278PercentDefaultPrec()
	case 279:
		return s.state279PercentPureParser()
	case 280:
		return s.state280PercentErrorVerbose()
	case 281:
		return s.state281PercentFilePrefix()
	case 282:
		return s.state282PercentInitialAction()
	case 283:
		return s.state283BracedCode()
	case 284:
		return s.state284PercentNondeterministicParser()
	case 285:
		return s.state285PercentNoDefaultPrec()
	case 286:
		return s.state286PercentDefaultPrec()
	case 287:
		return s.state287PercentErrorVerbose()
	case 288:
		return s.state288PercentInitialAction()
	case 289:
		return s.state289BracedCode()
	case 290:
		return s.state290PercentNondeterministicParser()
	case 291:
		return s.state291PercentNoDefaultPrec()
	case 292:
		return s.state292PercentErrorVerbose()
	case 293:
		return s.state293PercentInitialAction()
	case 294:
		return s.state294BracedCode()
	case 295:
		return s.state295PercentNondeterministicParser()
	case 296:
		return s.state296PercentNoDefaultPrec()
	case 297:
		return s.state297PercentInitialAction()
	case 298:
		return s.state298BracedCode()
	case 299:
		return s.state299PercentNondeterministicParser()
	case 300:
		return s.state300PercentNoDefaultPrec()
	case 301:
		return s.state301BracedCode()
	case 302:
		return s.state302PercentNondeterministicParser()
	case 303:
		return s.state303BracedCode()
	case 304:
		return s.state304PercentNondeterministicParser()
	case 305:
		return s.state305BracedCode()
	case 306:
		return s.state306PercentNondeterministicParser()
	case 307:
		return s.state307PercentNondeterministicParser()
	case 308:
		return s.state308PercentNondeterministicParser()
	case 309:
		return s.state309PercentNondeterministicParser()
	case 310:
		return s.state310PercentNondeterministicParser()
	case 311:
		return s.state311PercentNondeterministicParser()
	default:
		return fmt.Errorf("unexpected scanner state %d", s.state)
	}
}

func (s *Scanner) state0Ws() error {

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '\t':
		s.state = 1
		return nil
	case nextRune == '\n':
		s.state = 1
		return nil
	case nextRune == '\r':
		s.state = 2
		return nil
	case nextRune == ' ':
		s.state = 1
		return nil
	case nextRune == '"':
		s.state = 4
		return nil
	case nextRune == '%':
		s.state = 6
		return nil
	case nextRune == '/':
		s.state = 3
		return nil
	case '0' <= nextRune && nextRune <= '9':
		s.state = 11
		return nil
	case nextRune == ':':
		s.state = 7
		return nil
	case nextRune == ';':
		s.state = 10
		return nil
	case nextRune == '=':
		s.state = 8
		return nil
	case nextRune == '_':
		s.state = 5
		return nil
	case nextRune == '|':
		s.state = 9
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state1Ws() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalWs
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '\t':
		s.state = 1
		return nil
	case nextRune == '\n':
		s.state = 1
		return nil
	case nextRune == '\r':
		s.state = 2
		return nil
	case nextRune == ' ':
		s.state = 1
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state2Ws() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '\n':
		s.state = 1
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state3Comment() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '*':
		s.state = 12
		return nil
	case nextRune == '/':
		s.state = 13
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state4String() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case 0x0 <= nextRune && nextRune <= '!':
		s.state = 4
		return nil
	case nextRune == '"':
		s.state = 15
		return nil
	case '#' <= nextRune && nextRune <= '[':
		s.state = 4
		return nil
	case nextRune == '\\':
		s.state = 14
		return nil
	case ']' <= nextRune && nextRune <= 0x10ffff:
		s.state = 4
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state5Tstring() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '(':
		s.state = 16
		return nil
	case nextRune == '_':
		s.state = 17
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state6PercentToken() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '%':
		s.state = 36
		return nil
	case nextRune == '<':
		s.state = 28
		return nil
	case nextRune == 'c':
		s.state = 25
		return nil
	case nextRune == 'd':
		s.state = 20
		return nil
	case nextRune == 'e':
		s.state = 26
		return nil
	case nextRune == 'f':
		s.state = 27
		return nil
	case nextRune == 'g':
		s.state = 29
		return nil
	case nextRune == 'h':
		s.state = 30
		return nil
	case nextRune == 'i':
		s.state = 31
		return nil
	case nextRune == 'l':
		s.state = 22
		return nil
	case nextRune == 'm':
		s.state = 24
		return nil
	case nextRune == 'n':
		s.state = 19
		return nil
	case nextRune == 'o':
		s.state = 32
		return nil
	case nextRune == 'p':
		s.state = 21
		return nil
	case nextRune == 'r':
		s.state = 23
		return nil
	case nextRune == 's':
		s.state = 33
		return nil
	case nextRune == 't':
		s.state = 18
		return nil
	case nextRune == 'u':
		s.state = 37
		return nil
	case nextRune == 'v':
		s.state = 34
		return nil
	case nextRune == 'y':
		s.state = 35
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state7Colon() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalColon
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state8Equal() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalEqual
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state9Pipe() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPipe
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state10Semicolon() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalSemicolon
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state11IntLiteral() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalIntLiteral
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case '0' <= nextRune && nextRune <= '9':
		s.state = 11
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state12Comment() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case 0x0 <= nextRune && nextRune <= ')':
		s.state = 12
		return nil
	case nextRune == '*':
		s.state = 38
		return nil
	case '+' <= nextRune && nextRune <= 0x10ffff:
		s.state = 12
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state13Comment() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalComment
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case 0x0 <= nextRune && nextRune <= '\t':
		s.state = 13
		return nil
	case 0xb <= nextRune && nextRune <= 0x10ffff:
		s.state = 13
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state14String() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case 0x0 <= nextRune && nextRune <= '!':
		s.state = 4
		return nil
	case nextRune == '"':
		s.state = 39
		return nil
	case '#' <= nextRune && nextRune <= '[':
		s.state = 4
		return nil
	case nextRune == '\\':
		s.state = 14
		return nil
	case ']' <= nextRune && nextRune <= 0x10ffff:
		s.state = 4
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state15String() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalString
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state16Tstring() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '"':
		s.state = 40
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state17BracedCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'n':
		s.state = 41
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state18PercentToken() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'o':
		s.state = 42
		return nil
	case nextRune == 'y':
		s.state = 43
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state19PercentNterm() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 46
		return nil
	case nextRune == 'o':
		s.state = 45
		return nil
	case nextRune == 't':
		s.state = 44
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state20PercentDestructor() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 47
		return nil
	case nextRune == 'p':
		s.state = 48
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state21PercentPrinter() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 51
		return nil
	case nextRune == 'r':
		s.state = 49
		return nil
	case nextRune == 'u':
		s.state = 50
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state22PercentLeft() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 53
		return nil
	case nextRune == 'e':
		s.state = 52
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state23PercentRight() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 55
		return nil
	case nextRune == 'i':
		s.state = 54
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state24PercentMerge() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 56
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state25PercentCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'o':
		s.state = 57
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state26PercentErrorVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'm':
		s.state = 60
		return nil
	case nextRune == 'r':
		s.state = 58
		return nil
	case nextRune == 'x':
		s.state = 59
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state27PercentFilePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'i':
		s.state = 61
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state28PercentFlag() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'f':
		s.state = 62
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state29PercentGlrParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'l':
		s.state = 63
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state30PercentHeader() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 64
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state31PercentInitialAction() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'n':
		s.state = 65
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state32PercentOutput() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'u':
		s.state = 66
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state33PercentSkeleton() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'k':
		s.state = 67
		return nil
	case nextRune == 't':
		s.state = 68
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state34PercentVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 69
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state35PercentYacc() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 70
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state36PercentPercent() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentPercent
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state37PercentUnion() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'n':
		s.state = 71
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state38Comment() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case 0x0 <= nextRune && nextRune <= '.':
		s.state = 12
		return nil
	case nextRune == '/':
		s.state = 72
		return nil
	case '0' <= nextRune && nextRune <= 0x10ffff:
		s.state = 12
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state39String() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalString
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case 0x0 <= nextRune && nextRune <= '!':
		s.state = 4
		return nil
	case nextRune == '"':
		s.state = 15
		return nil
	case '#' <= nextRune && nextRune <= '[':
		s.state = 4
		return nil
	case nextRune == '\\':
		s.state = 14
		return nil
	case ']' <= nextRune && nextRune <= 0x10ffff:
		s.state = 4
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state40Tstring() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case 0x0 <= nextRune && nextRune <= '!':
		s.state = 40
		return nil
	case nextRune == '"':
		s.state = 74
		return nil
	case '#' <= nextRune && nextRune <= '[':
		s.state = 40
		return nil
	case nextRune == '\\':
		s.state = 73
		return nil
	case ']' <= nextRune && nextRune <= 0x10ffff:
		s.state = 40
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state41BracedCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'o':
		s.state = 75
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state42PercentToken() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'k':
		s.state = 76
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state43PercentType() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'p':
		s.state = 77
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state44PercentNterm() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 78
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state45PercentNonassoc() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '-':
		s.state = 80
		return nil
	case nextRune == 'n':
		s.state = 79
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state46PercentNamePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'm':
		s.state = 81
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state47PercentDestructor() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'f':
		s.state = 83
		return nil
	case nextRune == 's':
		s.state = 82
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state48PercentDprec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 84
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state49PercentPrinter() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 86
		return nil
	case nextRune == 'i':
		s.state = 85
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state50PercentPureParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 87
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state51PercentParam() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 88
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state52PercentLeft() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'f':
		s.state = 89
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state53PercentLanguage() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'n':
		s.state = 90
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state54PercentRight() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'g':
		s.state = 91
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state55PercentRequire() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'q':
		s.state = 92
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state56PercentMerge() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 93
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state57PercentCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'd':
		s.state = 94
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state58PercentErrorVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 95
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state59PercentExpect() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'p':
		s.state = 96
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state60PercentEmpty() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'p':
		s.state = 97
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state61PercentFilePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'l':
		s.state = 98
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state62PercentFlag() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'l':
		s.state = 99
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state63PercentGlrParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 100
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state64PercentHeader() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 101
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state65PercentInitialAction() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'i':
		s.state = 102
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state66PercentOutput() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 103
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state67PercentSkeleton() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 104
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state68PercentStart() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 105
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state69PercentVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 106
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state70PercentYacc() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'c':
		s.state = 107
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state71PercentUnion() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'i':
		s.state = 108
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state72Comment() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalComment
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state73Tstring() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case 0x0 <= nextRune && nextRune <= '!':
		s.state = 40
		return nil
	case nextRune == '"':
		s.state = 109
		return nil
	case '#' <= nextRune && nextRune <= '[':
		s.state = 40
		return nil
	case nextRune == '\\':
		s.state = 73
		return nil
	case ']' <= nextRune && nextRune <= 0x10ffff:
		s.state = 40
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state74Tstring() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == ')':
		s.state = 110
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state75BracedCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 111
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state76PercentToken() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 112
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state77PercentType() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 113
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state78PercentNterm() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 114
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state79PercentNonassoc() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 115
		return nil
	case nextRune == 'd':
		s.state = 116
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state80PercentNoDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'd':
		s.state = 117
		return nil
	case nextRune == 'l':
		s.state = 118
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state81PercentNamePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 119
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state82PercentDestructor() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 120
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state83PercentDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 121
		return nil
	case nextRune == 'i':
		s.state = 122
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state84PercentDprec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 123
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state85PercentPrinter() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'n':
		s.state = 124
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state86PercentPrecedence() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'c':
		s.state = 125
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state87PercentPureParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 126
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state88PercentParam() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 127
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state89PercentLeft() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 128
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state90PercentLanguage() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'g':
		s.state = 129
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state91PercentRight() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'h':
		s.state = 130
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state92PercentRequire() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'u':
		s.state = 131
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state93PercentMerge() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'g':
		s.state = 132
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state94PercentCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 133
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state95PercentErrorVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'o':
		s.state = 134
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state96PercentExpect() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 135
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state97PercentEmpty() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 136
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state98PercentFilePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 137
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state99PercentFlag() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 138
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state100PercentGlrParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '-':
		s.state = 139
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state101PercentHeader() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'd':
		s.state = 140
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state102PercentInitialAction() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 141
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state103PercentOutput() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'p':
		s.state = 142
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state104PercentSkeleton() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'l':
		s.state = 143
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state105PercentStart() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 144
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state106PercentVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'b':
		s.state = 145
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state107PercentYacc() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'c':
		s.state = 146
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state108PercentUnion() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'o':
		s.state = 147
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state109Tstring() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case 0x0 <= nextRune && nextRune <= '!':
		s.state = 40
		return nil
	case nextRune == '"':
		s.state = 74
		return nil
	case '#' <= nextRune && nextRune <= '(':
		s.state = 40
		return nil
	case nextRune == ')':
		s.state = 148
		return nil
	case '*' <= nextRune && nextRune <= '[':
		s.state = 40
		return nil
	case nextRune == '\\':
		s.state = 73
		return nil
	case ']' <= nextRune && nextRune <= 0x10ffff:
		s.state = 40
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state110Tstring() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalTstring
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state111BracedCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '_':
		s.state = 149
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state112PercentToken() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'n':
		s.state = 150
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state113PercentType() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentType
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state114PercentNterm() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'm':
		s.state = 151
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state115PercentNonassoc() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 's':
		s.state = 152
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state116PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 153
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state117PercentNoDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 154
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state118PercentNoLines() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'i':
		s.state = 155
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state119PercentNamePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '-':
		s.state = 156
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state120PercentDestructor() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 157
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state121PercentDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'u':
		s.state = 158
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state122PercentDefine() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'n':
		s.state = 159
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state123PercentDprec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'c':
		s.state = 160
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state124PercentPrinter() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 161
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state125PercentPrec() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentPrec
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 162
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state126PercentPureParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '-':
		s.state = 163
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state127PercentParam() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'm':
		s.state = 164
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state128PercentLeft() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentLeft
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state129PercentLanguage() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'u':
		s.state = 165
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state130PercentRight() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 166
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state131PercentRequire() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'i':
		s.state = 167
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state132PercentMerge() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 168
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state133PercentCode() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentCode
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state134PercentErrorVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 169
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state135PercentExpect() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'c':
		s.state = 170
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state136PercentEmpty() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'y':
		s.state = 171
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state137PercentFilePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '-':
		s.state = 172
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state138PercentFlag() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'g':
		s.state = 173
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state139PercentGlrParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'p':
		s.state = 174
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state140PercentHeader() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 175
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state141PercentInitialAction() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'i':
		s.state = 176
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state142PercentOutput() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'u':
		s.state = 177
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state143PercentSkeleton() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 178
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state144PercentStart() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 179
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state145PercentVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'o':
		s.state = 180
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state146PercentYacc() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentYacc
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state147PercentUnion() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'n':
		s.state = 181
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state148Tstring() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalTstring
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case 0x0 <= nextRune && nextRune <= '!':
		s.state = 40
		return nil
	case nextRune == '"':
		s.state = 74
		return nil
	case '#' <= nextRune && nextRune <= '[':
		s.state = 40
		return nil
	case nextRune == '\\':
		s.state = 73
		return nil
	case ']' <= nextRune && nextRune <= 0x10ffff:
		s.state = 40
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state149BracedCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'i':
		s.state = 182
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state150PercentToken() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentToken
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '-':
		s.state = 183
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state151PercentNterm() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentNterm
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state152PercentNonassoc() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 's':
		s.state = 184
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state153PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 185
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state154PercentNoDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'f':
		s.state = 186
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state155PercentNoLines() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'n':
		s.state = 187
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state156PercentNamePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'p':
		s.state = 188
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state157PercentDestructor() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'u':
		s.state = 189
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state158PercentDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'l':
		s.state = 190
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state159PercentDefine() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 191
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state160PercentDprec() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentDprec
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state161PercentPrinter() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 192
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state162PercentPrecedence() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'd':
		s.state = 193
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state163PercentPureParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'p':
		s.state = 194
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state164PercentParam() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentParam
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state165PercentLanguage() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 195
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state166PercentRight() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentRight
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state167PercentRequire() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 196
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state168PercentMerge() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentMerge
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state169PercentErrorVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '-':
		s.state = 197
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state170PercentExpect() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 198
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state171PercentEmpty() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentEmpty
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state172PercentFilePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'p':
		s.state = 199
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state173PercentFlag() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '>':
		s.state = 200
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state174PercentGlrParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 201
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state175PercentHeader() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 202
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state176PercentInitialAction() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 203
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state177PercentOutput() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 204
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state178PercentSkeleton() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 205
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state179PercentStart() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentStart
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state180PercentVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 's':
		s.state = 206
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state181PercentUnion() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentUnion
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state182BracedCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'm':
		s.state = 207
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state183PercentTokenTable() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 208
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state184PercentNonassoc() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'o':
		s.state = 209
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state185PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 210
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state186PercentNoDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 211
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state187PercentNoLines() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 212
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state188PercentNamePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 213
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state189PercentDestructor() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'c':
		s.state = 214
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state190PercentDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 215
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state191PercentDefine() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentDefine
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state192PercentPrinter() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 216
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state193PercentPrecedence() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 217
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state194PercentPureParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 218
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state195PercentLanguage() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'g':
		s.state = 219
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state196PercentRequire() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 220
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state197PercentErrorVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'v':
		s.state = 221
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state198PercentExpect() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentExpect
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '-':
		s.state = 222
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state199PercentFilePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 223
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state200PercentFlag() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentFlag
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state201PercentGlrParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 224
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state202PercentHeader() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentHeader
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state203PercentInitialAction() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'l':
		s.state = 225
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state204PercentOutput() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentOutput
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state205PercentSkeleton() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'o':
		s.state = 226
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state206PercentVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 227
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state207BracedCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'p':
		s.state = 228
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state208PercentTokenTable() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 229
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state209PercentNonassoc() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'c':
		s.state = 230
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state210PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 231
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state211PercentNoDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'u':
		s.state = 232
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state212PercentNoLines() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 's':
		s.state = 233
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state213PercentNamePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 234
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state214PercentDestructor() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 235
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state215PercentDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '-':
		s.state = 236
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state216PercentPrinter() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentPrinter
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state217PercentPrecedence() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'n':
		s.state = 237
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state218PercentPureParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 238
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state219PercentLanguage() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 239
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state220PercentRequire() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentRequire
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state221PercentErrorVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 240
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state222PercentExpectRr() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 241
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state223PercentFilePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 242
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state224PercentGlrParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 's':
		s.state = 243
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state225PercentInitialAction() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '-':
		s.state = 244
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state226PercentSkeleton() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'n':
		s.state = 245
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state227PercentVerbose() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentVerbose
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state228BracedCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'l':
		s.state = 246
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state229PercentTokenTable() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'b':
		s.state = 247
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state230PercentNonassoc() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentNonassoc
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state231PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'm':
		s.state = 248
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state232PercentNoDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'l':
		s.state = 249
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state233PercentNoLines() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentNoLines
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state234PercentNamePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'f':
		s.state = 250
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state235PercentDestructor() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'o':
		s.state = 251
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state236PercentDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'p':
		s.state = 252
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state237PercentPrecedence() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'c':
		s.state = 253
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state238PercentPureParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 's':
		s.state = 254
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state239PercentLanguage() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentLanguage
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state240PercentErrorVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 255
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state241PercentExpectRr() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 256
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state242PercentFilePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'f':
		s.state = 257
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state243PercentGlrParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 258
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state244PercentInitialAction() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 259
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state245PercentSkeleton() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentSkeleton
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state246BracedCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 260
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state247PercentTokenTable() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'l':
		s.state = 261
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state248PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'i':
		s.state = 262
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state249PercentNoDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 263
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state250PercentNamePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'i':
		s.state = 264
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state251PercentDestructor() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 265
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state252PercentDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 266
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state253PercentPrecedence() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 267
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state254PercentPureParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 268
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state255PercentErrorVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'b':
		s.state = 269
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state256PercentExpectRr() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentExpectRr
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state257PercentFilePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'i':
		s.state = 270
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state258PercentGlrParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 271
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state259PercentInitialAction() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'c':
		s.state = 272
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state260BracedCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'm':
		s.state = 273
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state261PercentTokenTable() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 274
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state262PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'n':
		s.state = 275
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state263PercentNoDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '-':
		s.state = 276
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state264PercentNamePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'x':
		s.state = 277
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state265PercentDestructor() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentDestructor
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state266PercentDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 278
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state267PercentPrecedence() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentPrecedence
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state268PercentPureParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 279
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state269PercentErrorVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'o':
		s.state = 280
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state270PercentFilePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'x':
		s.state = 281
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state271PercentGlrParser() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentGlrParser
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state272PercentInitialAction() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 282
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state273BracedCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 283
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state274PercentTokenTable() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentTokenTable
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state275PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'i':
		s.state = 284
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state276PercentNoDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'p':
		s.state = 285
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state277PercentNamePrefix() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentNamePrefix
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state278PercentDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'c':
		s.state = 286
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state279PercentPureParser() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentPureParser
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state280PercentErrorVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 's':
		s.state = 287
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state281PercentFilePrefix() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentFilePrefix
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state282PercentInitialAction() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'i':
		s.state = 288
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state283BracedCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'n':
		s.state = 289
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state284PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 's':
		s.state = 290
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state285PercentNoDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 291
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state286PercentDefaultPrec() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentDefaultPrec
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state287PercentErrorVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 292
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state288PercentInitialAction() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'o':
		s.state = 293
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state289BracedCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 294
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state290PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 295
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state291PercentNoDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 296
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state292PercentErrorVerbose() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentErrorVerbose
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state293PercentInitialAction() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'n':
		s.state = 297
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state294BracedCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 298
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state295PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'i':
		s.state = 299
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state296PercentNoDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'c':
		s.state = 300
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state297PercentInitialAction() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentInitialAction
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state298BracedCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'd':
		s.state = 301
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state299PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'c':
		s.state = 302
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state300PercentNoDefaultPrec() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentNoDefaultPrec
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state301BracedCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '_':
		s.state = 303
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state302PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '-':
		s.state = 304
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state303BracedCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '_':
		s.state = 305
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state304PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'p':
		s.state = 306
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state305BracedCode() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalBracedCode
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state306PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 307
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state307PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 308
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state308PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 's':
		s.state = 309
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state309PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 310
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state310PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 311
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state311PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TerminalPercentNondeterministicParser
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}
