// Code generated by GoLR. DO NOT EDIT.
package parser

import (
	"errors"
	"fmt"
	"golr/pkg/runtime"
)

var (
	// ErrInvalidRune is an error which is returned when no transition matches the current rune.
	ErrInvalidRune = errors.New("invalid rune")
)

// Token is the data type representing all terminal symbols.
type Token int

const (
	TokenWs                            Token = 0
	TokenComment                       Token = 1
	TokenString                        Token = 2
	TokenTstring                       Token = 3
	TokenPercentToken                  Token = 4
	TokenPercentNterm                  Token = 5
	TokenPercentType                   Token = 6
	TokenPercentDestructor             Token = 7
	TokenPercentPrinter                Token = 8
	TokenPercentLeft                   Token = 9
	TokenPercentRight                  Token = 10
	TokenPercentNonassoc               Token = 11
	TokenPercentPrecedence             Token = 12
	TokenPercentPrec                   Token = 13
	TokenPercentDprec                  Token = 14
	TokenPercentMerge                  Token = 15
	TokenPercentCode                   Token = 16
	TokenPercentDefaultPrec            Token = 17
	TokenPercentDefine                 Token = 18
	TokenPercentErrorVerbose           Token = 19
	TokenPercentExpect                 Token = 20
	TokenPercentExpectRr               Token = 21
	TokenPercentFilePrefix             Token = 22
	TokenPercentFlag                   Token = 23
	TokenPercentGlrParser              Token = 24
	TokenPercentHeader                 Token = 25
	TokenPercentInitialAction          Token = 26
	TokenPercentLanguage               Token = 27
	TokenPercentNamePrefix             Token = 28
	TokenPercentNoDefaultPrec          Token = 29
	TokenPercentNoLines                Token = 30
	TokenPercentNondeterministicParser Token = 31
	TokenPercentOutput                 Token = 32
	TokenPercentPureParser             Token = 33
	TokenPercentRequire                Token = 34
	TokenPercentSkeleton               Token = 35
	TokenPercentStart                  Token = 36
	TokenPercentTokenTable             Token = 37
	TokenPercentVerbose                Token = 38
	TokenPercentYacc                   Token = 39
	TokenBracedCode                    Token = 40
	TokenBracedPredicate               Token = 41
	TokenCharLiteral                   Token = 42
	TokenColon                         Token = 43
	TokenEpilogue                      Token = 44
	TokenEqual                         Token = 45
	TokenId                            Token = 46
	TokenBracketedId                   Token = 47
	TokenIdColon                       Token = 48
	TokenPercentPercent                Token = 49
	TokenPipe                          Token = 50
	TokenPrologue                      Token = 51
	TokenSemicolon                     Token = 52
	TokenTag                           Token = 53
	TokenTagAny                        Token = 54
	TokenTagNone                       Token = 55
	TokenIntLiteral                    Token = 56
	TokenPercentParam                  Token = 57
	TokenPercentUnion                  Token = 58
	TokenPercentEmpty                  Token = 59
	// InvalidToken is a terminal which does not exist. It is used for situations where no token was found yet.
	InvalidToken Token = ^Token(0)
	EndToken     Token = InvalidToken - 1
	TokenError   Token = InvalidToken - 2
)

// Token implements fmt.Stringer.
var _ fmt.Stringer = (*Token)(nil)

// String returns a string representation of the terminal.
func (t Token) String() string {
	switch t {
	case TokenWs:
		return `WS`
	case TokenComment:
		return `COMMENT`
	case TokenString:
		return `STRING`
	case TokenTstring:
		return `TSTRING`
	case TokenPercentToken:
		return `PERCENT_TOKEN`
	case TokenPercentNterm:
		return `PERCENT_NTERM`
	case TokenPercentType:
		return `PERCENT_TYPE`
	case TokenPercentDestructor:
		return `PERCENT_DESTRUCTOR`
	case TokenPercentPrinter:
		return `PERCENT_PRINTER`
	case TokenPercentLeft:
		return `PERCENT_LEFT`
	case TokenPercentRight:
		return `PERCENT_RIGHT`
	case TokenPercentNonassoc:
		return `PERCENT_NONASSOC`
	case TokenPercentPrecedence:
		return `PERCENT_PRECEDENCE`
	case TokenPercentPrec:
		return `PERCENT_PREC`
	case TokenPercentDprec:
		return `PERCENT_DPREC`
	case TokenPercentMerge:
		return `PERCENT_MERGE`
	case TokenPercentCode:
		return `PERCENT_CODE`
	case TokenPercentDefaultPrec:
		return `PERCENT_DEFAULT_PREC`
	case TokenPercentDefine:
		return `PERCENT_DEFINE`
	case TokenPercentErrorVerbose:
		return `PERCENT_ERROR_VERBOSE`
	case TokenPercentExpect:
		return `PERCENT_EXPECT`
	case TokenPercentExpectRr:
		return `PERCENT_EXPECT_RR`
	case TokenPercentFilePrefix:
		return `PERCENT_FILE_PREFIX`
	case TokenPercentFlag:
		return `PERCENT_FLAG`
	case TokenPercentGlrParser:
		return `PERCENT_GLR_PARSER`
	case TokenPercentHeader:
		return `PERCENT_HEADER`
	case TokenPercentInitialAction:
		return `PERCENT_INITIAL_ACTION`
	case TokenPercentLanguage:
		return `PERCENT_LANGUAGE`
	case TokenPercentNamePrefix:
		return `PERCENT_NAME_PREFIX`
	case TokenPercentNoDefaultPrec:
		return `PERCENT_NO_DEFAULT_PREC`
	case TokenPercentNoLines:
		return `PERCENT_NO_LINES`
	case TokenPercentNondeterministicParser:
		return `PERCENT_NONDETERMINISTIC_PARSER`
	case TokenPercentOutput:
		return `PERCENT_OUTPUT`
	case TokenPercentPureParser:
		return `PERCENT_PURE_PARSER`
	case TokenPercentRequire:
		return `PERCENT_REQUIRE`
	case TokenPercentSkeleton:
		return `PERCENT_SKELETON`
	case TokenPercentStart:
		return `PERCENT_START`
	case TokenPercentTokenTable:
		return `PERCENT_TOKEN_TABLE`
	case TokenPercentVerbose:
		return `PERCENT_VERBOSE`
	case TokenPercentYacc:
		return `PERCENT_YACC`
	case TokenBracedCode:
		return `BRACED_CODE`
	case TokenBracedPredicate:
		return `BRACED_PREDICATE`
	case TokenCharLiteral:
		return `CHAR_LITERAL`
	case TokenColon:
		return `COLON`
	case TokenEpilogue:
		return `EPILOGUE`
	case TokenEqual:
		return `EQUAL`
	case TokenId:
		return `ID`
	case TokenBracketedId:
		return `BRACKETED_ID`
	case TokenIdColon:
		return `ID_COLON`
	case TokenPercentPercent:
		return `PERCENT_PERCENT`
	case TokenPipe:
		return `PIPE`
	case TokenPrologue:
		return `PROLOGUE`
	case TokenSemicolon:
		return `SEMICOLON`
	case TokenTag:
		return `TAG`
	case TokenTagAny:
		return `TAG_ANY`
	case TokenTagNone:
		return `TAG_NONE`
	case TokenIntLiteral:
		return `INT_LITERAL`
	case TokenPercentParam:
		return `PERCENT_PARAM`
	case TokenPercentUnion:
		return `PERCENT_UNION`
	case TokenPercentEmpty:
		return `PERCENT_EMPTY`
	case InvalidToken:
		return "invalid token"
	case EndToken:
		return "end token"
	case TokenError:
		return "token error"
	default:
		return "unknown"
	}
}

// Scanner implements the scanner and returns tokens.
type Scanner struct {
	runeReader runtime.UTF8RuneReader
	tokenStart runtime.UTF8RuneReader
	tokenEnd   runtime.UTF8RuneReader

	state int
	token Token

	err      error
	filePath string
}

// NewScanner creates a new instance of the scanner with the given rune reader. The filePath parameter is used to
// return information about which file has an error. You can provide any string you want if you don't have a file.
func NewScanner(runeReader runtime.UTF8RuneReader, filePath string) *Scanner {
	// consume the first rune here, as the start state does not call Next() on its own
	_ = runeReader.Next()
	return &Scanner{
		runeReader: runeReader,
		tokenStart: runeReader,
		tokenEnd:   runeReader,
		token:      InvalidToken,
		filePath:   filePath,
	}
}

// Err returns the error which occurred on the last call to Next().
// It will return io.EOF after Next() has returned false.
func (s *Scanner) Err() error {
	return s.err
}

// Token returns the current token.
func (s *Scanner) Token() Token {
	return s.token
}

// ByteOffset returns the start of the token in bytes from the source start.
// It returns the location after the last byte after the call to Next() returned false.
func (s *Scanner) ByteOffset() int {
	return s.tokenStart.ByteOffset()
}

// Line returns the line the token is located on.
func (s *Scanner) Line() int {
	return s.tokenStart.Line()
}

// Column returns the column the token is located on.
func (s *Scanner) Column() int {
	return s.tokenStart.Column()
}

// Lexeme returns the bytes which make up the token.
func (s *Scanner) Lexeme() []byte {
	return s.tokenStart.Lexeme(s.tokenStart.ByteOffset(), s.tokenEnd.ByteOffset())
}

// FilePath returns the file path of the file being parsed. This information can be useful in error messages.
func (s *Scanner) FilePath() string {
	return s.filePath
}

// Next consumes characters until it found the next token.
func (s *Scanner) Next() bool {
	s.err = nil
	s.tokenStart = s.tokenEnd
	s.runeReader = s.tokenEnd
	s.state = 0

	for s.err == nil {
		s.err = s.dispatchState()
	}

	if s.tokenStart.ByteOffset() != s.tokenEnd.ByteOffset() {
		s.err = nil
		return true
	}
	s.token = EndToken
	return false
}

func (s *Scanner) dispatchState() error {
	switch s.state {
	case 0:
		return s.state0Ws()
	case 1:
		return s.state1Ws()
	case 2:
		return s.state2Ws()
	case 3:
		return s.state3Comment()
	case 4:
		return s.state4String()
	case 5:
		return s.state5Tstring()
	case 6:
		return s.state6PercentToken()
	case 7:
		return s.state7Colon()
	case 8:
		return s.state8Equal()
	case 9:
		return s.state9Id()
	case 10:
		return s.state10BracketedId()
	case 11:
		return s.state11Pipe()
	case 12:
		return s.state12Semicolon()
	case 13:
		return s.state13IntLiteral()
	case 14:
		return s.state14Comment()
	case 15:
		return s.state15Comment()
	case 16:
		return s.state16String()
	case 17:
		return s.state17String()
	case 18:
		return s.state18Tstring()
	case 19:
		return s.state19BracedCode()
	case 20:
		return s.state20PercentToken()
	case 21:
		return s.state21PercentNterm()
	case 22:
		return s.state22PercentDestructor()
	case 23:
		return s.state23PercentPrinter()
	case 24:
		return s.state24PercentLeft()
	case 25:
		return s.state25PercentRight()
	case 26:
		return s.state26PercentMerge()
	case 27:
		return s.state27PercentCode()
	case 28:
		return s.state28PercentErrorVerbose()
	case 29:
		return s.state29PercentFilePrefix()
	case 30:
		return s.state30PercentFlag()
	case 31:
		return s.state31PercentGlrParser()
	case 32:
		return s.state32PercentHeader()
	case 33:
		return s.state33PercentInitialAction()
	case 34:
		return s.state34PercentOutput()
	case 35:
		return s.state35PercentSkeleton()
	case 36:
		return s.state36PercentVerbose()
	case 37:
		return s.state37PercentYacc()
	case 38:
		return s.state38PercentPercent()
	case 39:
		return s.state39PercentUnion()
	case 40:
		return s.state40BracketedId()
	case 41:
		return s.state41Comment()
	case 42:
		return s.state42String()
	case 43:
		return s.state43Tstring()
	case 44:
		return s.state44BracedCode()
	case 45:
		return s.state45IdColon()
	case 46:
		return s.state46PercentToken()
	case 47:
		return s.state47PercentType()
	case 48:
		return s.state48PercentNterm()
	case 49:
		return s.state49PercentNonassoc()
	case 50:
		return s.state50PercentNamePrefix()
	case 51:
		return s.state51PercentDestructor()
	case 52:
		return s.state52PercentDprec()
	case 53:
		return s.state53PercentPrinter()
	case 54:
		return s.state54PercentPureParser()
	case 55:
		return s.state55PercentParam()
	case 56:
		return s.state56PercentLeft()
	case 57:
		return s.state57PercentLanguage()
	case 58:
		return s.state58PercentRight()
	case 59:
		return s.state59PercentRequire()
	case 60:
		return s.state60PercentMerge()
	case 61:
		return s.state61PercentCode()
	case 62:
		return s.state62PercentErrorVerbose()
	case 63:
		return s.state63PercentExpect()
	case 64:
		return s.state64PercentEmpty()
	case 65:
		return s.state65PercentFilePrefix()
	case 66:
		return s.state66PercentFlag()
	case 67:
		return s.state67PercentGlrParser()
	case 68:
		return s.state68PercentHeader()
	case 69:
		return s.state69PercentInitialAction()
	case 70:
		return s.state70PercentOutput()
	case 71:
		return s.state71PercentSkeleton()
	case 72:
		return s.state72PercentStart()
	case 73:
		return s.state73PercentVerbose()
	case 74:
		return s.state74PercentYacc()
	case 75:
		return s.state75PercentUnion()
	case 76:
		return s.state76BracketedId()
	case 77:
		return s.state77Comment()
	case 78:
		return s.state78Tstring()
	case 79:
		return s.state79Tstring()
	case 80:
		return s.state80BracedCode()
	case 81:
		return s.state81IdColon()
	case 82:
		return s.state82PercentToken()
	case 83:
		return s.state83PercentType()
	case 84:
		return s.state84PercentNterm()
	case 85:
		return s.state85PercentNonassoc()
	case 86:
		return s.state86PercentNoDefaultPrec()
	case 87:
		return s.state87PercentNamePrefix()
	case 88:
		return s.state88PercentDestructor()
	case 89:
		return s.state89PercentDefaultPrec()
	case 90:
		return s.state90PercentDprec()
	case 91:
		return s.state91PercentPrinter()
	case 92:
		return s.state92PercentPrecedence()
	case 93:
		return s.state93PercentPureParser()
	case 94:
		return s.state94PercentParam()
	case 95:
		return s.state95PercentLeft()
	case 96:
		return s.state96PercentLanguage()
	case 97:
		return s.state97PercentRight()
	case 98:
		return s.state98PercentRequire()
	case 99:
		return s.state99PercentMerge()
	case 100:
		return s.state100PercentCode()
	case 101:
		return s.state101PercentErrorVerbose()
	case 102:
		return s.state102PercentExpect()
	case 103:
		return s.state103PercentEmpty()
	case 104:
		return s.state104PercentFilePrefix()
	case 105:
		return s.state105PercentFlag()
	case 106:
		return s.state106PercentGlrParser()
	case 107:
		return s.state107PercentHeader()
	case 108:
		return s.state108PercentInitialAction()
	case 109:
		return s.state109PercentOutput()
	case 110:
		return s.state110PercentSkeleton()
	case 111:
		return s.state111PercentStart()
	case 112:
		return s.state112PercentVerbose()
	case 113:
		return s.state113PercentYacc()
	case 114:
		return s.state114PercentUnion()
	case 115:
		return s.state115Tstring()
	case 116:
		return s.state116Tstring()
	case 117:
		return s.state117BracedCode()
	case 118:
		return s.state118IdColon()
	case 119:
		return s.state119PercentToken()
	case 120:
		return s.state120PercentType()
	case 121:
		return s.state121PercentNterm()
	case 122:
		return s.state122PercentNonassoc()
	case 123:
		return s.state123PercentNondeterministicParser()
	case 124:
		return s.state124PercentNoDefaultPrec()
	case 125:
		return s.state125PercentNoLines()
	case 126:
		return s.state126PercentNamePrefix()
	case 127:
		return s.state127PercentDestructor()
	case 128:
		return s.state128PercentDefaultPrec()
	case 129:
		return s.state129PercentDefine()
	case 130:
		return s.state130PercentDprec()
	case 131:
		return s.state131PercentPrinter()
	case 132:
		return s.state132PercentPrec()
	case 133:
		return s.state133PercentPureParser()
	case 134:
		return s.state134PercentParam()
	case 135:
		return s.state135PercentLeft()
	case 136:
		return s.state136PercentLanguage()
	case 137:
		return s.state137PercentRight()
	case 138:
		return s.state138PercentRequire()
	case 139:
		return s.state139PercentMerge()
	case 140:
		return s.state140PercentCode()
	case 141:
		return s.state141PercentErrorVerbose()
	case 142:
		return s.state142PercentExpect()
	case 143:
		return s.state143PercentEmpty()
	case 144:
		return s.state144PercentFilePrefix()
	case 145:
		return s.state145PercentFlag()
	case 146:
		return s.state146PercentGlrParser()
	case 147:
		return s.state147PercentHeader()
	case 148:
		return s.state148PercentInitialAction()
	case 149:
		return s.state149PercentOutput()
	case 150:
		return s.state150PercentSkeleton()
	case 151:
		return s.state151PercentStart()
	case 152:
		return s.state152PercentVerbose()
	case 153:
		return s.state153PercentYacc()
	case 154:
		return s.state154PercentUnion()
	case 155:
		return s.state155Tstring()
	case 156:
		return s.state156BracedCode()
	case 157:
		return s.state157IdColon()
	case 158:
		return s.state158PercentToken()
	case 159:
		return s.state159PercentNterm()
	case 160:
		return s.state160PercentNonassoc()
	case 161:
		return s.state161PercentNondeterministicParser()
	case 162:
		return s.state162PercentNoDefaultPrec()
	case 163:
		return s.state163PercentNoLines()
	case 164:
		return s.state164PercentNamePrefix()
	case 165:
		return s.state165PercentDestructor()
	case 166:
		return s.state166PercentDefaultPrec()
	case 167:
		return s.state167PercentDefine()
	case 168:
		return s.state168PercentDprec()
	case 169:
		return s.state169PercentPrinter()
	case 170:
		return s.state170PercentPrecedence()
	case 171:
		return s.state171PercentPureParser()
	case 172:
		return s.state172PercentParam()
	case 173:
		return s.state173PercentLanguage()
	case 174:
		return s.state174PercentRight()
	case 175:
		return s.state175PercentRequire()
	case 176:
		return s.state176PercentMerge()
	case 177:
		return s.state177PercentErrorVerbose()
	case 178:
		return s.state178PercentExpect()
	case 179:
		return s.state179PercentEmpty()
	case 180:
		return s.state180PercentFilePrefix()
	case 181:
		return s.state181PercentFlag()
	case 182:
		return s.state182PercentGlrParser()
	case 183:
		return s.state183PercentHeader()
	case 184:
		return s.state184PercentInitialAction()
	case 185:
		return s.state185PercentOutput()
	case 186:
		return s.state186PercentSkeleton()
	case 187:
		return s.state187PercentStart()
	case 188:
		return s.state188PercentVerbose()
	case 189:
		return s.state189PercentUnion()
	case 190:
		return s.state190BracedCode()
	case 191:
		return s.state191IdColon()
	case 192:
		return s.state192PercentTokenTable()
	case 193:
		return s.state193PercentNonassoc()
	case 194:
		return s.state194PercentNondeterministicParser()
	case 195:
		return s.state195PercentNoDefaultPrec()
	case 196:
		return s.state196PercentNoLines()
	case 197:
		return s.state197PercentNamePrefix()
	case 198:
		return s.state198PercentDestructor()
	case 199:
		return s.state199PercentDefaultPrec()
	case 200:
		return s.state200PercentDefine()
	case 201:
		return s.state201PercentPrinter()
	case 202:
		return s.state202PercentPrecedence()
	case 203:
		return s.state203PercentPureParser()
	case 204:
		return s.state204PercentLanguage()
	case 205:
		return s.state205PercentRequire()
	case 206:
		return s.state206PercentErrorVerbose()
	case 207:
		return s.state207PercentExpect()
	case 208:
		return s.state208PercentFilePrefix()
	case 209:
		return s.state209PercentFlag()
	case 210:
		return s.state210PercentGlrParser()
	case 211:
		return s.state211PercentHeader()
	case 212:
		return s.state212PercentInitialAction()
	case 213:
		return s.state213PercentOutput()
	case 214:
		return s.state214PercentSkeleton()
	case 215:
		return s.state215PercentVerbose()
	case 216:
		return s.state216BracedCode()
	case 217:
		return s.state217IdColon()
	case 218:
		return s.state218PercentTokenTable()
	case 219:
		return s.state219PercentNonassoc()
	case 220:
		return s.state220PercentNondeterministicParser()
	case 221:
		return s.state221PercentNoDefaultPrec()
	case 222:
		return s.state222PercentNoLines()
	case 223:
		return s.state223PercentNamePrefix()
	case 224:
		return s.state224PercentDestructor()
	case 225:
		return s.state225PercentDefaultPrec()
	case 226:
		return s.state226PercentPrinter()
	case 227:
		return s.state227PercentPrecedence()
	case 228:
		return s.state228PercentPureParser()
	case 229:
		return s.state229PercentLanguage()
	case 230:
		return s.state230PercentRequire()
	case 231:
		return s.state231PercentErrorVerbose()
	case 232:
		return s.state232PercentExpectRr()
	case 233:
		return s.state233PercentFilePrefix()
	case 234:
		return s.state234PercentGlrParser()
	case 235:
		return s.state235PercentInitialAction()
	case 236:
		return s.state236PercentSkeleton()
	case 237:
		return s.state237PercentVerbose()
	case 238:
		return s.state238BracedCode()
	case 239:
		return s.state239IdColon()
	case 240:
		return s.state240PercentTokenTable()
	case 241:
		return s.state241PercentNonassoc()
	case 242:
		return s.state242PercentNondeterministicParser()
	case 243:
		return s.state243PercentNoDefaultPrec()
	case 244:
		return s.state244PercentNoLines()
	case 245:
		return s.state245PercentNamePrefix()
	case 246:
		return s.state246PercentDestructor()
	case 247:
		return s.state247PercentDefaultPrec()
	case 248:
		return s.state248PercentPrecedence()
	case 249:
		return s.state249PercentPureParser()
	case 250:
		return s.state250PercentLanguage()
	case 251:
		return s.state251PercentErrorVerbose()
	case 252:
		return s.state252PercentExpectRr()
	case 253:
		return s.state253PercentFilePrefix()
	case 254:
		return s.state254PercentGlrParser()
	case 255:
		return s.state255PercentInitialAction()
	case 256:
		return s.state256PercentSkeleton()
	case 257:
		return s.state257BracedCode()
	case 258:
		return s.state258IdColon()
	case 259:
		return s.state259PercentTokenTable()
	case 260:
		return s.state260PercentNondeterministicParser()
	case 261:
		return s.state261PercentNoDefaultPrec()
	case 262:
		return s.state262PercentNamePrefix()
	case 263:
		return s.state263PercentDestructor()
	case 264:
		return s.state264PercentDefaultPrec()
	case 265:
		return s.state265PercentPrecedence()
	case 266:
		return s.state266PercentPureParser()
	case 267:
		return s.state267PercentErrorVerbose()
	case 268:
		return s.state268PercentExpectRr()
	case 269:
		return s.state269PercentFilePrefix()
	case 270:
		return s.state270PercentGlrParser()
	case 271:
		return s.state271PercentInitialAction()
	case 272:
		return s.state272BracedCode()
	case 273:
		return s.state273IdColon()
	case 274:
		return s.state274PercentTokenTable()
	case 275:
		return s.state275PercentNondeterministicParser()
	case 276:
		return s.state276PercentNoDefaultPrec()
	case 277:
		return s.state277PercentNamePrefix()
	case 278:
		return s.state278PercentDestructor()
	case 279:
		return s.state279PercentDefaultPrec()
	case 280:
		return s.state280PercentPrecedence()
	case 281:
		return s.state281PercentPureParser()
	case 282:
		return s.state282PercentErrorVerbose()
	case 283:
		return s.state283PercentFilePrefix()
	case 284:
		return s.state284PercentGlrParser()
	case 285:
		return s.state285PercentInitialAction()
	case 286:
		return s.state286BracedCode()
	case 287:
		return s.state287IdColon()
	case 288:
		return s.state288PercentTokenTable()
	case 289:
		return s.state289PercentNondeterministicParser()
	case 290:
		return s.state290PercentNoDefaultPrec()
	case 291:
		return s.state291PercentNamePrefix()
	case 292:
		return s.state292PercentDefaultPrec()
	case 293:
		return s.state293PercentPureParser()
	case 294:
		return s.state294PercentErrorVerbose()
	case 295:
		return s.state295PercentFilePrefix()
	case 296:
		return s.state296PercentInitialAction()
	case 297:
		return s.state297BracedCode()
	case 298:
		return s.state298IdColon()
	case 299:
		return s.state299PercentNondeterministicParser()
	case 300:
		return s.state300PercentNoDefaultPrec()
	case 301:
		return s.state301PercentDefaultPrec()
	case 302:
		return s.state302PercentErrorVerbose()
	case 303:
		return s.state303PercentInitialAction()
	case 304:
		return s.state304BracedCode()
	case 305:
		return s.state305IdColon()
	case 306:
		return s.state306PercentNondeterministicParser()
	case 307:
		return s.state307PercentNoDefaultPrec()
	case 308:
		return s.state308PercentErrorVerbose()
	case 309:
		return s.state309PercentInitialAction()
	case 310:
		return s.state310BracedCode()
	case 311:
		return s.state311IdColon()
	case 312:
		return s.state312PercentNondeterministicParser()
	case 313:
		return s.state313PercentNoDefaultPrec()
	case 314:
		return s.state314PercentInitialAction()
	case 315:
		return s.state315BracedCode()
	case 316:
		return s.state316IdColon()
	case 317:
		return s.state317PercentNondeterministicParser()
	case 318:
		return s.state318PercentNoDefaultPrec()
	case 319:
		return s.state319BracedCode()
	case 320:
		return s.state320IdColon()
	case 321:
		return s.state321PercentNondeterministicParser()
	case 322:
		return s.state322BracedCode()
	case 323:
		return s.state323IdColon()
	case 324:
		return s.state324PercentNondeterministicParser()
	case 325:
		return s.state325BracedCode()
	case 326:
		return s.state326IdColon()
	case 327:
		return s.state327PercentNondeterministicParser()
	case 328:
		return s.state328IdColon()
	case 329:
		return s.state329PercentNondeterministicParser()
	case 330:
		return s.state330IdColon()
	case 331:
		return s.state331PercentNondeterministicParser()
	case 332:
		return s.state332IdColon()
	case 333:
		return s.state333PercentNondeterministicParser()
	case 334:
		return s.state334IdColon()
	case 335:
		return s.state335PercentNondeterministicParser()
	case 336:
		return s.state336IdColon()
	case 337:
		return s.state337PercentNondeterministicParser()
	case 338:
		return s.state338IdColon()
	case 339:
		return s.state339IdColon()
	case 340:
		return s.state340IdColon()
	case 341:
		return s.state341IdColon()
	case 342:
		return s.state342IdColon()
	case 343:
		return s.state343IdColon()
	case 344:
		return s.state344IdColon()
	case 345:
		return s.state345IdColon()
	case 346:
		return s.state346IdColon()
	case 347:
		return s.state347IdColon()
	case 348:
		return s.state348IdColon()
	case 349:
		return s.state349IdColon()
	default:
		return fmt.Errorf("unexpected scanner state %d", s.state)
	}
}

func (s *Scanner) state0Ws() error {

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '\t':
		s.state = 1
		return nil
	case nextRune == '\n':
		s.state = 1
		return nil
	case nextRune == '\r':
		s.state = 2
		return nil
	case nextRune == ' ':
		s.state = 1
		return nil
	case nextRune == '"':
		s.state = 4
		return nil
	case nextRune == '%':
		s.state = 6
		return nil
	case nextRune == '/':
		s.state = 3
		return nil
	case '0' <= nextRune && nextRune <= '9':
		s.state = 13
		return nil
	case nextRune == ':':
		s.state = 7
		return nil
	case nextRune == ';':
		s.state = 12
		return nil
	case nextRune == '=':
		s.state = 8
		return nil
	case 'A' <= nextRune && nextRune <= 'Z':
		s.state = 9
		return nil
	case nextRune == '[':
		s.state = 10
		return nil
	case nextRune == '_':
		s.state = 5
		return nil
	case 'a' <= nextRune && nextRune <= 'z':
		s.state = 9
		return nil
	case nextRune == '|':
		s.state = 11
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state1Ws() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenWs
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '\t':
		s.state = 1
		return nil
	case nextRune == '\n':
		s.state = 1
		return nil
	case nextRune == '\r':
		s.state = 2
		return nil
	case nextRune == ' ':
		s.state = 1
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state2Ws() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '\n':
		s.state = 1
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state3Comment() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '*':
		s.state = 14
		return nil
	case nextRune == '/':
		s.state = 15
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state4String() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case 0x0 <= nextRune && nextRune <= '!':
		s.state = 4
		return nil
	case nextRune == '"':
		s.state = 17
		return nil
	case '#' <= nextRune && nextRune <= '[':
		s.state = 4
		return nil
	case nextRune == '\\':
		s.state = 16
		return nil
	case ']' <= nextRune && nextRune <= 0x10ffff:
		s.state = 4
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state5Tstring() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '(':
		s.state = 18
		return nil
	case nextRune == '_':
		s.state = 19
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state6PercentToken() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '%':
		s.state = 38
		return nil
	case nextRune == '<':
		s.state = 30
		return nil
	case nextRune == 'c':
		s.state = 27
		return nil
	case nextRune == 'd':
		s.state = 22
		return nil
	case nextRune == 'e':
		s.state = 28
		return nil
	case nextRune == 'f':
		s.state = 29
		return nil
	case nextRune == 'g':
		s.state = 31
		return nil
	case nextRune == 'h':
		s.state = 32
		return nil
	case nextRune == 'i':
		s.state = 33
		return nil
	case nextRune == 'l':
		s.state = 24
		return nil
	case nextRune == 'm':
		s.state = 26
		return nil
	case nextRune == 'n':
		s.state = 21
		return nil
	case nextRune == 'o':
		s.state = 34
		return nil
	case nextRune == 'p':
		s.state = 23
		return nil
	case nextRune == 'r':
		s.state = 25
		return nil
	case nextRune == 's':
		s.state = 35
		return nil
	case nextRune == 't':
		s.state = 20
		return nil
	case nextRune == 'u':
		s.state = 39
		return nil
	case nextRune == 'v':
		s.state = 36
		return nil
	case nextRune == 'y':
		s.state = 37
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state7Colon() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenColon
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state8Equal() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenEqual
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state9Id() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenId
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '.':
		s.state = 9
		return nil
	case '0' <= nextRune && nextRune <= '9':
		s.state = 9
		return nil
	case 'A' <= nextRune && nextRune <= 'Z':
		s.state = 9
		return nil
	case nextRune == '_':
		s.state = 9
		return nil
	case 'a' <= nextRune && nextRune <= 'z':
		s.state = 9
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state10BracketedId() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case 'A' <= nextRune && nextRune <= 'Z':
		s.state = 40
		return nil
	case 'a' <= nextRune && nextRune <= 'z':
		s.state = 40
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state11Pipe() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPipe
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state12Semicolon() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenSemicolon
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state13IntLiteral() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenIntLiteral
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case '0' <= nextRune && nextRune <= '9':
		s.state = 13
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state14Comment() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case 0x0 <= nextRune && nextRune <= ')':
		s.state = 14
		return nil
	case nextRune == '*':
		s.state = 41
		return nil
	case '+' <= nextRune && nextRune <= 0x10ffff:
		s.state = 14
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state15Comment() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenComment
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case 0x0 <= nextRune && nextRune <= '\t':
		s.state = 15
		return nil
	case 0xb <= nextRune && nextRune <= 0x10ffff:
		s.state = 15
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state16String() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case 0x0 <= nextRune && nextRune <= '!':
		s.state = 4
		return nil
	case nextRune == '"':
		s.state = 42
		return nil
	case '#' <= nextRune && nextRune <= '[':
		s.state = 4
		return nil
	case nextRune == '\\':
		s.state = 16
		return nil
	case ']' <= nextRune && nextRune <= 0x10ffff:
		s.state = 4
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state17String() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenString
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state18Tstring() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '"':
		s.state = 43
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state19BracedCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'i':
		s.state = 45
		return nil
	case nextRune == 'n':
		s.state = 44
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state20PercentToken() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'o':
		s.state = 46
		return nil
	case nextRune == 'y':
		s.state = 47
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state21PercentNterm() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 50
		return nil
	case nextRune == 'o':
		s.state = 49
		return nil
	case nextRune == 't':
		s.state = 48
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state22PercentDestructor() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 51
		return nil
	case nextRune == 'p':
		s.state = 52
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state23PercentPrinter() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 55
		return nil
	case nextRune == 'r':
		s.state = 53
		return nil
	case nextRune == 'u':
		s.state = 54
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state24PercentLeft() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 57
		return nil
	case nextRune == 'e':
		s.state = 56
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state25PercentRight() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 59
		return nil
	case nextRune == 'i':
		s.state = 58
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state26PercentMerge() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 60
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state27PercentCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'o':
		s.state = 61
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state28PercentErrorVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'm':
		s.state = 64
		return nil
	case nextRune == 'r':
		s.state = 62
		return nil
	case nextRune == 'x':
		s.state = 63
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state29PercentFilePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'i':
		s.state = 65
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state30PercentFlag() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'f':
		s.state = 66
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state31PercentGlrParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'l':
		s.state = 67
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state32PercentHeader() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 68
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state33PercentInitialAction() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'n':
		s.state = 69
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state34PercentOutput() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'u':
		s.state = 70
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state35PercentSkeleton() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'k':
		s.state = 71
		return nil
	case nextRune == 't':
		s.state = 72
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state36PercentVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 73
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state37PercentYacc() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 74
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state38PercentPercent() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentPercent
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state39PercentUnion() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'n':
		s.state = 75
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state40BracketedId() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '.':
		s.state = 40
		return nil
	case '0' <= nextRune && nextRune <= '9':
		s.state = 40
		return nil
	case 'A' <= nextRune && nextRune <= 'Z':
		s.state = 40
		return nil
	case nextRune == ']':
		s.state = 76
		return nil
	case nextRune == '_':
		s.state = 40
		return nil
	case 'a' <= nextRune && nextRune <= 'z':
		s.state = 40
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state41Comment() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case 0x0 <= nextRune && nextRune <= '.':
		s.state = 14
		return nil
	case nextRune == '/':
		s.state = 77
		return nil
	case '0' <= nextRune && nextRune <= 0x10ffff:
		s.state = 14
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state42String() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenString
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case 0x0 <= nextRune && nextRune <= '!':
		s.state = 4
		return nil
	case nextRune == '"':
		s.state = 17
		return nil
	case '#' <= nextRune && nextRune <= '[':
		s.state = 4
		return nil
	case nextRune == '\\':
		s.state = 16
		return nil
	case ']' <= nextRune && nextRune <= 0x10ffff:
		s.state = 4
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state43Tstring() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case 0x0 <= nextRune && nextRune <= '!':
		s.state = 43
		return nil
	case nextRune == '"':
		s.state = 79
		return nil
	case '#' <= nextRune && nextRune <= '[':
		s.state = 43
		return nil
	case nextRune == '\\':
		s.state = 78
		return nil
	case ']' <= nextRune && nextRune <= 0x10ffff:
		s.state = 43
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state44BracedCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'o':
		s.state = 80
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state45IdColon() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'm':
		s.state = 81
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state46PercentToken() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'k':
		s.state = 82
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state47PercentType() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'p':
		s.state = 83
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state48PercentNterm() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 84
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state49PercentNonassoc() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '-':
		s.state = 86
		return nil
	case nextRune == 'n':
		s.state = 85
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state50PercentNamePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'm':
		s.state = 87
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state51PercentDestructor() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'f':
		s.state = 89
		return nil
	case nextRune == 's':
		s.state = 88
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state52PercentDprec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 90
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state53PercentPrinter() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 92
		return nil
	case nextRune == 'i':
		s.state = 91
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state54PercentPureParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 93
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state55PercentParam() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 94
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state56PercentLeft() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'f':
		s.state = 95
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state57PercentLanguage() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'n':
		s.state = 96
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state58PercentRight() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'g':
		s.state = 97
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state59PercentRequire() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'q':
		s.state = 98
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state60PercentMerge() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 99
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state61PercentCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'd':
		s.state = 100
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state62PercentErrorVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 101
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state63PercentExpect() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'p':
		s.state = 102
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state64PercentEmpty() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'p':
		s.state = 103
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state65PercentFilePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'l':
		s.state = 104
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state66PercentFlag() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'l':
		s.state = 105
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state67PercentGlrParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 106
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state68PercentHeader() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 107
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state69PercentInitialAction() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'i':
		s.state = 108
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state70PercentOutput() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 109
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state71PercentSkeleton() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 110
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state72PercentStart() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 111
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state73PercentVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 112
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state74PercentYacc() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'c':
		s.state = 113
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state75PercentUnion() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'i':
		s.state = 114
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state76BracketedId() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenBracketedId
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state77Comment() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenComment
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state78Tstring() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case 0x0 <= nextRune && nextRune <= '!':
		s.state = 43
		return nil
	case nextRune == '"':
		s.state = 115
		return nil
	case '#' <= nextRune && nextRune <= '[':
		s.state = 43
		return nil
	case nextRune == '\\':
		s.state = 78
		return nil
	case ']' <= nextRune && nextRune <= 0x10ffff:
		s.state = 43
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state79Tstring() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == ')':
		s.state = 116
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state80BracedCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 117
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state81IdColon() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'p':
		s.state = 118
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state82PercentToken() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 119
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state83PercentType() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 120
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state84PercentNterm() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 121
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state85PercentNonassoc() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 122
		return nil
	case nextRune == 'd':
		s.state = 123
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state86PercentNoDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'd':
		s.state = 124
		return nil
	case nextRune == 'l':
		s.state = 125
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state87PercentNamePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 126
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state88PercentDestructor() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 127
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state89PercentDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 128
		return nil
	case nextRune == 'i':
		s.state = 129
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state90PercentDprec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 130
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state91PercentPrinter() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'n':
		s.state = 131
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state92PercentPrecedence() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'c':
		s.state = 132
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state93PercentPureParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 133
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state94PercentParam() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 134
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state95PercentLeft() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 135
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state96PercentLanguage() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'g':
		s.state = 136
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state97PercentRight() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'h':
		s.state = 137
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state98PercentRequire() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'u':
		s.state = 138
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state99PercentMerge() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'g':
		s.state = 139
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state100PercentCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 140
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state101PercentErrorVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'o':
		s.state = 141
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state102PercentExpect() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 142
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state103PercentEmpty() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 143
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state104PercentFilePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 144
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state105PercentFlag() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 145
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state106PercentGlrParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '-':
		s.state = 146
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state107PercentHeader() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'd':
		s.state = 147
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state108PercentInitialAction() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 148
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state109PercentOutput() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'p':
		s.state = 149
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state110PercentSkeleton() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'l':
		s.state = 150
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state111PercentStart() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 151
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state112PercentVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'b':
		s.state = 152
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state113PercentYacc() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'c':
		s.state = 153
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state114PercentUnion() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'o':
		s.state = 154
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state115Tstring() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case 0x0 <= nextRune && nextRune <= '!':
		s.state = 43
		return nil
	case nextRune == '"':
		s.state = 79
		return nil
	case '#' <= nextRune && nextRune <= '(':
		s.state = 43
		return nil
	case nextRune == ')':
		s.state = 155
		return nil
	case '*' <= nextRune && nextRune <= '[':
		s.state = 43
		return nil
	case nextRune == '\\':
		s.state = 78
		return nil
	case ']' <= nextRune && nextRune <= 0x10ffff:
		s.state = 43
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state116Tstring() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenTstring
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state117BracedCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '_':
		s.state = 156
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state118IdColon() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'l':
		s.state = 157
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state119PercentToken() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'n':
		s.state = 158
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state120PercentType() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentType
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state121PercentNterm() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'm':
		s.state = 159
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state122PercentNonassoc() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 's':
		s.state = 160
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state123PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 161
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state124PercentNoDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 162
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state125PercentNoLines() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'i':
		s.state = 163
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state126PercentNamePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '-':
		s.state = 164
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state127PercentDestructor() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 165
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state128PercentDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'u':
		s.state = 166
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state129PercentDefine() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'n':
		s.state = 167
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state130PercentDprec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'c':
		s.state = 168
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state131PercentPrinter() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 169
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state132PercentPrec() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentPrec
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 170
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state133PercentPureParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '-':
		s.state = 171
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state134PercentParam() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'm':
		s.state = 172
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state135PercentLeft() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentLeft
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state136PercentLanguage() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'u':
		s.state = 173
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state137PercentRight() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 174
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state138PercentRequire() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'i':
		s.state = 175
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state139PercentMerge() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 176
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state140PercentCode() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentCode
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state141PercentErrorVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 177
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state142PercentExpect() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'c':
		s.state = 178
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state143PercentEmpty() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'y':
		s.state = 179
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state144PercentFilePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '-':
		s.state = 180
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state145PercentFlag() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'g':
		s.state = 181
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state146PercentGlrParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'p':
		s.state = 182
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state147PercentHeader() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 183
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state148PercentInitialAction() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'i':
		s.state = 184
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state149PercentOutput() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'u':
		s.state = 185
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state150PercentSkeleton() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 186
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state151PercentStart() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 187
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state152PercentVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'o':
		s.state = 188
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state153PercentYacc() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentYacc
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state154PercentUnion() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'n':
		s.state = 189
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state155Tstring() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenTstring
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case 0x0 <= nextRune && nextRune <= '!':
		s.state = 43
		return nil
	case nextRune == '"':
		s.state = 79
		return nil
	case '#' <= nextRune && nextRune <= '[':
		s.state = 43
		return nil
	case nextRune == '\\':
		s.state = 78
		return nil
	case ']' <= nextRune && nextRune <= 0x10ffff:
		s.state = 43
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state156BracedCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'i':
		s.state = 190
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state157IdColon() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 191
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state158PercentToken() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentToken
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '-':
		s.state = 192
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state159PercentNterm() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentNterm
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state160PercentNonassoc() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 's':
		s.state = 193
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state161PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 194
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state162PercentNoDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'f':
		s.state = 195
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state163PercentNoLines() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'n':
		s.state = 196
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state164PercentNamePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'p':
		s.state = 197
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state165PercentDestructor() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'u':
		s.state = 198
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state166PercentDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'l':
		s.state = 199
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state167PercentDefine() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 200
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state168PercentDprec() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentDprec
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state169PercentPrinter() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 201
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state170PercentPrecedence() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'd':
		s.state = 202
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state171PercentPureParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'p':
		s.state = 203
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state172PercentParam() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentParam
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state173PercentLanguage() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 204
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state174PercentRight() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentRight
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state175PercentRequire() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 205
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state176PercentMerge() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentMerge
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state177PercentErrorVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '-':
		s.state = 206
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state178PercentExpect() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 207
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state179PercentEmpty() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentEmpty
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state180PercentFilePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'p':
		s.state = 208
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state181PercentFlag() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '>':
		s.state = 209
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state182PercentGlrParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 210
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state183PercentHeader() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 211
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state184PercentInitialAction() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 212
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state185PercentOutput() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 213
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state186PercentSkeleton() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 214
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state187PercentStart() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentStart
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state188PercentVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 's':
		s.state = 215
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state189PercentUnion() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentUnion
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state190BracedCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'm':
		s.state = 216
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state191IdColon() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'm':
		s.state = 217
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state192PercentTokenTable() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 218
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state193PercentNonassoc() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'o':
		s.state = 219
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state194PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 220
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state195PercentNoDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 221
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state196PercentNoLines() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 222
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state197PercentNamePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 223
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state198PercentDestructor() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'c':
		s.state = 224
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state199PercentDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 225
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state200PercentDefine() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentDefine
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state201PercentPrinter() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 226
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state202PercentPrecedence() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 227
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state203PercentPureParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 228
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state204PercentLanguage() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'g':
		s.state = 229
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state205PercentRequire() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 230
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state206PercentErrorVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'v':
		s.state = 231
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state207PercentExpect() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentExpect
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '-':
		s.state = 232
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state208PercentFilePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 233
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state209PercentFlag() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentFlag
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state210PercentGlrParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 234
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state211PercentHeader() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentHeader
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state212PercentInitialAction() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'l':
		s.state = 235
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state213PercentOutput() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentOutput
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state214PercentSkeleton() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'o':
		s.state = 236
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state215PercentVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 237
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state216BracedCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'p':
		s.state = 238
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state217IdColon() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 239
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state218PercentTokenTable() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 240
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state219PercentNonassoc() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'c':
		s.state = 241
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state220PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 242
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state221PercentNoDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'u':
		s.state = 243
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state222PercentNoLines() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 's':
		s.state = 244
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state223PercentNamePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 245
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state224PercentDestructor() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 246
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state225PercentDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '-':
		s.state = 247
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state226PercentPrinter() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentPrinter
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state227PercentPrecedence() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'n':
		s.state = 248
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state228PercentPureParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 249
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state229PercentLanguage() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 250
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state230PercentRequire() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentRequire
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state231PercentErrorVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 251
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state232PercentExpectRr() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 252
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state233PercentFilePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 253
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state234PercentGlrParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 's':
		s.state = 254
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state235PercentInitialAction() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '-':
		s.state = 255
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state236PercentSkeleton() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'n':
		s.state = 256
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state237PercentVerbose() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentVerbose
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state238BracedCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'l':
		s.state = 257
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state239IdColon() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'n':
		s.state = 258
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state240PercentTokenTable() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'b':
		s.state = 259
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state241PercentNonassoc() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentNonassoc
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state242PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'm':
		s.state = 260
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state243PercentNoDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'l':
		s.state = 261
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state244PercentNoLines() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentNoLines
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state245PercentNamePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'f':
		s.state = 262
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state246PercentDestructor() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'o':
		s.state = 263
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state247PercentDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'p':
		s.state = 264
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state248PercentPrecedence() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'c':
		s.state = 265
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state249PercentPureParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 's':
		s.state = 266
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state250PercentLanguage() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentLanguage
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state251PercentErrorVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 267
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state252PercentExpectRr() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 268
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state253PercentFilePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'f':
		s.state = 269
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state254PercentGlrParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 270
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state255PercentInitialAction() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 271
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state256PercentSkeleton() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentSkeleton
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state257BracedCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 272
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state258IdColon() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 273
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state259PercentTokenTable() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'l':
		s.state = 274
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state260PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'i':
		s.state = 275
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state261PercentNoDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 276
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state262PercentNamePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'i':
		s.state = 277
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state263PercentDestructor() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 278
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state264PercentDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 279
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state265PercentPrecedence() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 280
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state266PercentPureParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 281
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state267PercentErrorVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'b':
		s.state = 282
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state268PercentExpectRr() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentExpectRr
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state269PercentFilePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'i':
		s.state = 283
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state270PercentGlrParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 284
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state271PercentInitialAction() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'c':
		s.state = 285
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state272BracedCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'm':
		s.state = 286
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state273IdColon() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 287
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state274PercentTokenTable() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 288
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state275PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'n':
		s.state = 289
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state276PercentNoDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '-':
		s.state = 290
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state277PercentNamePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'x':
		s.state = 291
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state278PercentDestructor() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentDestructor
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state279PercentDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 292
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state280PercentPrecedence() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentPrecedence
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state281PercentPureParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 293
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state282PercentErrorVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'o':
		s.state = 294
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state283PercentFilePrefix() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'x':
		s.state = 295
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state284PercentGlrParser() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentGlrParser
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state285PercentInitialAction() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 296
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state286BracedCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 297
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state287IdColon() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'd':
		s.state = 298
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state288PercentTokenTable() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentTokenTable
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state289PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'i':
		s.state = 299
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state290PercentNoDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'p':
		s.state = 300
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state291PercentNamePrefix() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentNamePrefix
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state292PercentDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'c':
		s.state = 301
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state293PercentPureParser() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentPureParser
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state294PercentErrorVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 's':
		s.state = 302
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state295PercentFilePrefix() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentFilePrefix
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state296PercentInitialAction() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'i':
		s.state = 303
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state297BracedCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'n':
		s.state = 304
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state298IdColon() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '_':
		s.state = 305
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state299PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 's':
		s.state = 306
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state300PercentNoDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 307
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state301PercentDefaultPrec() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentDefaultPrec
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state302PercentErrorVerbose() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 308
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state303PercentInitialAction() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'o':
		s.state = 309
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state304BracedCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 310
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state305IdColon() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'b':
		s.state = 311
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state306PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 312
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state307PercentNoDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 313
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state308PercentErrorVerbose() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentErrorVerbose
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state309PercentInitialAction() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'n':
		s.state = 314
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state310BracedCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 315
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state311IdColon() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'y':
		s.state = 316
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state312PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'i':
		s.state = 317
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state313PercentNoDefaultPrec() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'c':
		s.state = 318
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state314PercentInitialAction() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentInitialAction
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state315BracedCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'd':
		s.state = 319
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state316IdColon() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '_':
		s.state = 320
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state317PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'c':
		s.state = 321
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state318PercentNoDefaultPrec() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentNoDefaultPrec
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state319BracedCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '_':
		s.state = 322
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state320IdColon() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 323
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state321PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '-':
		s.state = 324
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state322BracedCode() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '_':
		s.state = 325
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state323IdColon() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'o':
		s.state = 326
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state324PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'p':
		s.state = 327
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state325BracedCode() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenBracedCode
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state326IdColon() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'k':
		s.state = 328
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state327PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 329
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state328IdColon() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 330
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state329PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 331
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state330IdColon() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'n':
		s.state = 332
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state331PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 's':
		s.state = 333
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state332IdColon() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '_':
		s.state = 334
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state333PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 335
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state334IdColon() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 't':
		s.state = 336
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state335PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 337
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state336IdColon() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 338
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state337PercentNondeterministicParser() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenPercentNondeterministicParser
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}

func (s *Scanner) state338IdColon() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'a':
		s.state = 339
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state339IdColon() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'n':
		s.state = 340
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state340IdColon() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 's':
		s.state = 341
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state341IdColon() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'f':
		s.state = 342
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state342IdColon() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'o':
		s.state = 343
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state343IdColon() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 344
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state344IdColon() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'm':
		s.state = 345
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state345IdColon() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'e':
		s.state = 346
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state346IdColon() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == 'r':
		s.state = 347
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state347IdColon() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '_':
		s.state = 348
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state348IdColon() error {
	_ = s.runeReader.Next()

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	nextRune := s.runeReader.Rune()
	switch {
	case nextRune == '_':
		s.state = 349
		return nil
	default:
		return ErrInvalidRune
	}
}

func (s *Scanner) state349IdColon() error {
	_ = s.runeReader.Next()

	// We have an accepting state, update our bookkeeping.
	s.token = TokenIdColon
	s.tokenEnd = s.runeReader

	if s.runeReader.Err() != nil {
		return s.runeReader.Err()
	}
	return ErrInvalidRune
}
