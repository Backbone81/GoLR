# Scanner Generator Backend: Null

This backend writes nothing. See [scanner generator backends](scannergen-backend.md) for the other backends.

What it is good for is running the frontend and the core without paying for the output: checking in a build that a set
of rules still builds, and benchmarking the frontend and the core without a backend in the measurement.
