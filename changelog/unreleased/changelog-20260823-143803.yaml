description: 'BREAKING: Aligned the generated scanners and parsers across all language
  backends, so that the same data types, names, comments and code structure appear
  in each of them. The parse error now reports what it is about with an ErrorKind enum
  instead of a boolean flag, which changes the public interface of the Rust, Python,
  Java, C#, TypeScript and JavaScript backends.'
